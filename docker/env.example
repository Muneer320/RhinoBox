# Database Configuration
# Copy this file to .env and set your actual passwords in production

# PostgreSQL Configuration
POSTGRES_USER=rhinobox
POSTGRES_PASSWORD=changeme_in_production
POSTGRES_DB=rhinobox

# MongoDB Configuration
MONGO_USER=rhinobox
MONGO_PASSWORD=changeme_in_production
MONGO_DB=rhinobox

# Note: In production, use strong, unique passwords and store them securely
# Never commit .env files with real passwords to version control
